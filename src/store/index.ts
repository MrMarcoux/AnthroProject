import Vue from 'vue'
import Vuex from 'vuex'
import { Model } from '@/models/model'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    model: Model
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
